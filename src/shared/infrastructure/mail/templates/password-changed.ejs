<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Password changed - <%= appName %></title>
</head>
<body>
  <h2>Hello <%= name %>,</h2>
  <p>Your password on <%= appName %> was changed on <%= date %>.</p>
  <p>If you did not make this change, please reset your password immediately and contact support at
    <a href="mailto:<%= typeof supportEmail !== 'undefined' && supportEmail ? supportEmail : 'support@example.com' %>"><%= typeof supportEmail !== 'undefined' && supportEmail ? supportEmail : 'support@example.com' %></a>.</p>
  <p>&copy; <%= year %> <%= appName %>. All rights reserved.</p>
</body>
</html>

<!--* Variables -->
<%# Template locals documented in mail.types / callers %>
